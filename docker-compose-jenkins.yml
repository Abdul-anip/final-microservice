services:
  docker-dind:
    image: docker:dind
    container_name: jenkins-docker
    privileged: true
    mem_limit: 2g
    mem_reservation: 1g
    cpus: 2
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      - DOCKER_TLS_CERTDIR=/certs
    volumes:
      - jenkins-docker-certs:/certs/client
      - jenkins_home:/var/jenkins_home
    networks:
      jenkins-network:
        aliases:
          - docker
    command: --storage-driver=overlay2
    restart: unless-stopped

  jenkins:
    build: ./jenkins
    container_name: jenkins
    user: root
    mem_limit: 2g
    mem_reservation: 1g
    cpus: 2
    dns:
      - 8.8.8.8
      - 8.8.4.4
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - jenkins-docker-certs:/certs/client:ro
      - ./:/workspace
    environment:
      - DOCKER_HOST=tcp://docker:2376
      - DOCKER_CERT_PATH=/certs/client
      - DOCKER_TLS_VERIFY=1
    networks:
      - jenkins-network
      - elk
    depends_on:
      - docker-dind
    restart: unless-stopped

networks:
  jenkins-network:
    driver: bridge
  elk:
    external: true
    name: latihan_perpustakaan_elk_hanif

volumes:
  jenkins_home:
  jenkins-docker-certs:
